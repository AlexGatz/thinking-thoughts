const layoutStyle = {
    margin: 20,
    padding: 20,
    background-color: white
};

const Layout = (props) => (
  <div style={layoutStyle}>
    {props.children}
  </div>
);

export default Layout;
